#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
FRAMEWORK_DIR="$(cd "$SCRIPT_DIR/.." && pwd)"
export PYTHONPATH="$FRAMEWORK_DIR${PYTHONPATH:+:$PYTHONPATH}"
exec python3 -m kernel.runtime.route_cli "$@"
