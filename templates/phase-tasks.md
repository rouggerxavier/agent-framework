---
{
  "schema_version": 1,
  "phase": {
    "id": "{{PHASE_ID}}",
    "name": "{{PHASE_NAME}}"
  },
  "tasks": []
}
---

# {{PHASE_ID}} — {{PHASE_NAME}} Task Contracts

Every item in `tasks` must contain the complete contract defined by
`templates/task-contract.md`. Paths alone are not executable contracts.

